/*
    The serverConfigurator defines what configuration items will be stored for each app environment.

    TODO:
        session configuration
        environment variables
        port
        logger options (file, web service, ...)
        passport serialize / de-serialize user
        production settings for hubba-admin usage
 */
